EduSphere platform deployed successfully!

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Infrastructure:
  - postgres:5432
  - redis:6379
  - minio:9000 (API), minio:9001 (Console)

Microservices:
  - auth-service:8001
  - course-service:8002
  - content-service:8003
  - quiz-service:8004
  - api-gateway:8000
  - frontend:80

{{- if .Values.ingress.enabled }}

Access the platform:
  http://{{ .Values.ingress.host }}

Ingress routes:
{{- range .Values.ingress.paths }}
  - {{ .path }} -> {{ .serviceName }}:{{ .servicePort }}
{{- end }}
{{- else }}

Port-forward the API gateway:
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/api-gateway 8000:8000
{{- end }}

Verify all pods:
  kubectl get pods --namespace {{ .Release.Namespace }} -l app.kubernetes.io/part-of=edusphere

IMPORTANT: Replace placeholder secrets in values.yaml before production use.
