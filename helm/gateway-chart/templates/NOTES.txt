API Gateway deployed.

Service DNS: api-gateway:{{ .Values.service.port }}
Cluster DNS: api-gateway.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

{{- if .Values.ingress.enabled }}
Ingress: http://{{ .Values.ingress.host }}{{ .Values.ingress.path }}
{{- end }}

Check status:
  kubectl get deployment --namespace {{ .Release.Namespace }} api-gateway
