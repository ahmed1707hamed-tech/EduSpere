apiVersion: v1
kind: Service
metadata:
  name: {{ include "quiz-chart.fullname" . }}
  labels:
    {{- include "quiz-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "quiz-chart.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
