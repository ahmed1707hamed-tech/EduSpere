# Internal ClusterIP service for content-service
apiVersion: v1
kind: Service
metadata:
  name: content-service
  namespace: edusphere
  labels:
    app.kubernetes.io/name: content-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: edusphere
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: content-service
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
