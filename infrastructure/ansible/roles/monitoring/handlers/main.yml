---
# Monitoring role handlers

- name: Restart monitoring stack
  community.docker.docker_compose_v2:
    project_src: "{{ monitoring_dir }}"
    project_name: "{{ monitoring_compose_project }}"
    state: present
    recreate: always
  become: true
  become_user: "{{ app_user }}"
